#include <bits/stdc++.h>
using namespace std;
typedef unsigned long long int logt;

int main()
{
	int a, b;
	cin >> a >> b;
	double sum = sqrt(a * a + b * b);
	cout << fixed << setprecision(2) << sum;
	system("pause");
}